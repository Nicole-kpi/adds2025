#include <iostream>
#include <string>
#include "Reverser.h"

int main() {
    std::string characters;
    std::cin >> characters;  // 从标准输入读字符串
    Reverser r;
    //std::cout << r.reverseString(characters) << std::endl; // 直接输出
    return 0;
}

